apiVersion: v1
kind: Pod
metadata:
  name: stroppy-client
  namespace: default
spec:
  containers:
  - name: stroppy-client
    image: registry.gitlab.com/picodata/dockers/stroppy_builder:v1
  imagePullSecrets:
  - name: stroppy-secret

