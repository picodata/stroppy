apiVersion: v1
kind: Pod
metadata:
  name: stroppy-client
  namespace: default
spec:
  containers:
  - name: stroppy-client
    image: docker.binary.picodata.io/stroppy:latest
    command: ["/bin/sleep"]
    args: [ "infinity" ]
  imagePullSecrets:
  - name: stroppy-secret
  
